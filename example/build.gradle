repositories {
    mavenCentral()
}

apply plugin: 'com.android.application'

dependencies {
    compile project(":library")
}

android {
    compileSdkVersion 19
    buildToolsVersion "20"

    defaultConfig {
        minSdkVersion 8
        targetSdkVersion 19
    }
}
